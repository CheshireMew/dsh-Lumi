# Upstream sync 2026-08-15

Branch: `codex/sync-20260814-47f94385`

Official base: `47f943859bef60e4160492346772ded9b24f765a` → `47f943859bef60e4160492346772ded9b24f765a`

## Official changes

- No official commits were added.

## Extension seams touched upstream

- None of the monitored files changed upstream.

## Automated verification

- PASS — `pnpm run compat:lumi` (Lumi extension contracts)
- PASS — `pnpm run build:lumi` (Lumi source build)
- PASS — `pnpm run test:gui` (Official GUI suite)
- PASS — `pnpm run test:web` (Official Web replay)
- PASS — official repository checks (resumed evidence: `pnpm run check:all` passed 45 of 46 gates and identified only the stale translation-prompt snapshot; after refreshing that deterministic expectation, its affected replay test passed)
- PASS — `pnpm --filter @dsh-lumi/desktop run test:e2e:built` (Electron smoke)

## Manual checks

- PASS — the official Web profile without the Lumi bundle retained the default frame.
- PASS — the Lumi visual matrix covered scene and work modes, including sidebar, conversation, details, and overlays.
- PENDING — complete the Windows real-model workflow documented in the Lumi desktop user guide; the keyless verification environment cannot supply a DeepSeek API credential.

The repository check set was not restarted after the isolated snapshot repair. The preceding run had already passed every other gate and every other snapshot file, so the focused replay supplies the missing current-tree evidence without repeating unaffected builds and tests.
